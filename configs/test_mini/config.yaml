experiment_id: test_mini
environment:
  name: Hopper-v5
  max_episode_steps: 200
policy:
  algorithm: SAC
  seed: 42
  total_timesteps: 2000
  learning_rate: 0.0003
  batch_size: 64
  gamma: 0.99
  learning_starts: 100
  n_envs: 1
data_generation:
  n_batches: 1
  episodes_per_batch: 10
  deterministic_policy: false
  n_envs: 4
  validation_episodes_per_batch: 5
  seed: 42
value_estimators:
  training:
    max_epochs: 200
    batch_size: 64
    convergence_patience: 20
    convergence_threshold: 0.0001
    eval_frequency: 10
    gamma: 0.99
    episode_subsets: [5, 10]
    shuffle_frequency: 50
    seed: 42
  methods:
  - monte_carlo
  - td
network:
  hidden_sizes: [32, 32]
  activation: relu
  device: auto
logging:
  use_wandb: false
  wandb_project: pooling-eval
  wandb_entity: null
  log_frequency: 10
evaluation:
  eval_episodes: 5
  paired_states_n_pairs: 0
  seed: 42
